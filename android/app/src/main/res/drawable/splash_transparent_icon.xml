<?xml version="1.0" encoding="utf-8"?>
<!-- Empty transparent drawable used to hide the Android 12+ system splash
     icon so only the black background (then the Flutter gif splash) shows. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/transparent" />
</layer-list>
